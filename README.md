# PDAC R1 Resection Risk Calculator

> **Demonstration build.** The regression coefficients currently in `app/app.R`
> are arbitrary placeholders used to test deployment. The application does not
> yet produce valid risk estimates.

Preoperative prediction of R1 resection in pancreatic ductal adenocarcinoma.
Retrospective cohort study (2021–2025).


**Live calculator:** <!-- add URL once Pages is live -->

---

## Repository structure

```
.
├── app/
│   ├── app.R              # the Shiny application
│   └── www/               # optional header image
├── R/                     # analysis and validation scripts
├── docs/                  # generated by build.R — served by GitHub Pages
├── build.R                # builds the static shinylive version
├── .gitignore
├── LICENSE
└── README.md
```

---

## Deployment

The site is built and published automatically by GitHub Actions
(`.github/workflows/deploy.yml`) on every push to `main`. No local R
installation is required.

One-time setup: **Settings → Pages → Source → GitHub Actions**.

The workflow compiles the app with `shinylive`, so R runs inside the visitor's
browser via webR — no server needed.


---

## Data availability

**No patient data are included in this repository.** This is a retrospective
study conducted under a waiver of informed consent; individual-level data
cannot be shared publicly. De-identified data may be available from the
corresponding author on reasonable request.

---

## License

MIT — see `LICENSE`.
